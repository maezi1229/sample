# 作業用一時ファイル（仕入先PDF・OCR結果・レビューシート等、機密情報を含み得るため管理対象外）
/work/
__pycache__/
*.pyc

# 実データ（実在の客先名・仕入先名・金額など）を含みうる生成物・入力ファイルは、
# 置き場所を問わず一切コミットしない（work/以下に限らず、リポジトリ直下に
# 誤って置かれた場合も防ぐ多重の安全策）。テンプレート等のコード資産として
# 管理する空ファイルだけ、個別に明示的に許可する。
*.pdf
*.xlsx
*.xlsm
*.csv
!/quote_automation/assets/*.xlsx
